# Build Your MongoDB Database

This directory is dedicated for a MongoDB database.

To build your database, install MongoDB and run the following in the server directory:
```bash
$ mongod --dbpath ./data/db
```
